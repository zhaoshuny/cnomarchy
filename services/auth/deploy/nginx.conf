# CNomarchy Auth API Nginx 配置
# 将此文件放到 /etc/nginx/sites-available/ 然后软链接到 sites-enabled/

server {
    listen 80;
    server_name api.cnomarchy.local;  # 替换为实际域名

    # 重定向到 HTTPS（如果有 SSL 证书）
    # return 301 https://$host$request_uri;

    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # 超时设置
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # 健康检查端点
    location /health {
        proxy_pass http://127.0.0.1:8000/health;
        access_log off;
    }
}

# HTTPS 配置（使用 Let's Encrypt 证书时取消注释）
# server {
#     listen 443 ssl http2;
#     server_name api.cnomarchy.local;
#
#     ssl_certificate /etc/letsencrypt/live/api.cnomarchy.local/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.cnomarchy.local/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     client_max_body_size 10M;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
