# CNomarchy 傻瓜化部署指南

> 从零开始，一步步搭建属于你的中国版 Omarchy OS
> 本指南面向完全不懂技术的用户，每一步都有详细说明

---

## 📋 目录

1. [项目是什么](#1-项目是什么)
2. [你需要准备什么](#2-你需要准备什么)
3. [第一步：购买云服务器](#3-第一步购买云服务器)
4. [第二步：购买域名并解析](#4-第二步购买域名并解析)
5. [第三步：申请短信服务](#5-第三步申请短信服务)
6. [第四步：部署认证后端服务](#6-第四步部署认证后端服务)
7. [第五步：下载 CNomarchy 系统镜像](#7-第五步下载-cnomarchy-系统镜像)
8. [第六步：制作系统 U 盘](#8-第六步制作系统-u-盘)
9. [第七步：安装 CNomarchy 系统](#9-第七步安装-cnomarchy-系统)
10. [第八步：配置手机号登录](#10-第八步配置手机号登录)
11. [第九步：安装软件兼容层](#11-第九步安装软件兼容层)
12. [第十步：使用 AI Agent Harness](#12-第十步使用-ai-agent-harness)
13. [常见问题](#13-常见问题)

---

## 1. 项目是什么

**CNomarchy** 是一个中国版的 Omarchy OS，核心特点：

- 📱 **手机号作为唯一登录入口**：不需要记用户名密码，用手机号验证码登录
- 🤖 **AI 原生操作系统**：把操作系统做成 AI 可以读和写的系统，AI 能帮你管理电脑
- 🐧 **基于 Arch Linux + Hyprland**：轻量、快速、美观的桌面环境
- 🌐 **五层软件兼容**：原生 Linux、安卓 App、Windows 软件、网页应用、云桌面

---

## 2. 你需要准备什么

| 项目 | 用途 | 预算 | 备注 |
|------|------|------|------|
| 云服务器 | 运行认证后端服务 | 约 50-100 元/年 | 阿里云/腾讯云轻量应用服务器即可 |
| 域名 | 访问认证服务 | 约 30-50 元/年 | 比如 `api.yourname.com` |
| 短信服务 | 发送登录验证码 | 约 0.04 元/条 | 阿里云短信/腾讯云短信 |
| U 盘 | 安装系统 | 已有即可 | 至少 8GB，会被格式化 |
| 电脑 | 安装和运行系统 | 已有即可 | 建议 8GB 内存以上 |

**总预算：约 100-200 元/年**

---

## 3. 第一步：购买云服务器

### 3.1 选择云服务商

推荐使用 **阿里云轻量应用服务器** 或 **腾讯云轻量应用服务器**，新手友好，价格便宜。

### 3.2 购买步骤

1. 打开阿里云/腾讯云官网，注册账号并实名认证
2. 搜索"轻量应用服务器"
3. 选择配置：
   - **地域**：选离你最近的（比如上海、广州）
   - **镜像**：选 Ubuntu 22.04 或 Ubuntu 24.04
   - **配置**：2核2G 内存即可（最低配就行）
   - **带宽**：3Mbps 以上
   - **时长**：先买1个月试试，好用再续费
4. 点击购买，完成支付

### 3.3 记录服务器信息

购买成功后，在控制台找到你的服务器，记录以下信息：

- **公网 IP 地址**：比如 `123.45.67.89`
- **用户名**：一般是 `root`
- **密码**：你设置的密码（或重置密码）

---

## 4. 第二步：购买域名并解析

### 4.1 购买域名

1. 在阿里云/腾讯云搜索"域名注册"
2. 输入你想要的域名（比如 `yourname.com`），检查是否可用
3. 选择一个可用的域名，购买（.com 约 50 元/年，.cn 约 30 元/年）
4. 完成实名认证（必须，否则无法使用）

### 4.2 域名解析

1. 进入域名控制台，找到你刚买的域名
2. 点击"解析"或"DNS 解析"
3. 添加一条解析记录：
   - **记录类型**：A 记录
   - **主机记录**：`api`（这样你的域名就是 `api.yourname.com`）
   - **记录值**：你的服务器公网 IP（比如 `123.45.67.89`）
   - **TTL**：默认即可（600 或 10 分钟）
4. 点击保存

### 4.3 验证解析

等待 5-10 分钟后，打开命令提示符（Windows 按 Win+R，输入 `cmd`），输入：

```
ping api.yourname.com
```

如果显示你的服务器 IP，说明解析成功了。

---

## 5. 第三步：申请短信服务

### 5.1 开通短信服务

1. 在阿里云/腾讯云搜索"短信服务"
2. 点击"开通服务"（免费开通，按条计费）
3. 完成企业或个人认证

### 5.2 创建签名

1. 在短信服务控制台，找到"国内消息" → "签名管理"
2. 点击"添加签名"
3. 填写：
   - **签名名称**：比如 `CNomarchy`（2-12个字符，不能有特殊符号）
   - **签名类型**：选"通用"或"互联网/IT"
   - **申请说明**：写"用于用户登录验证码发送"
   - **证明材料**：如果有网站/APP 就上传，没有就选"无"
4. 提交审核，一般 1-2 小时通过

### 5.3 创建模板

1. 在"模板管理"中点击"添加模板"
2. 填写：
   - **模板名称**：`登录验证码`
   - **模板类型**：选"验证码"
   - **模板内容**：`您的验证码为：${code}，5分钟内有效，请勿泄露给他人。`
   - **申请说明**：用户登录时发送验证码
3. 提交审核，一般 1-2 小时通过

### 5.4 获取 API 密钥

1. 在控制台右上角，鼠标移到你的头像，点击"AccessKey 管理"
2. 点击"创建 AccessKey"
3. 记录：
   - **AccessKey ID**：比如 `LTAI5t...`
   - **AccessKey Secret**：比如 `abc123...`（只显示一次，一定要保存好！）

---

## 6. 第四步：部署认证后端服务

### 6.1 连接服务器

**Windows 用户**：

1. 按 Win+R，输入 `cmd`，打开命令提示符
2. 输入：
   ```
   ssh root@你的服务器IP
   ```
   比如：`ssh root@123.45.67.89`
3. 第一次连接会问你是否继续，输入 `yes` 回车
4. 输入服务器密码（输入时不会显示，输完回车）

**Mac 用户**：打开"终端"，同样输入上面的命令。

### 6.2 一键部署

连接成功后，依次输入以下命令（每行输完按回车）：

```bash
# 1. 下载项目代码
git clone https://github.com/zhaoshuny/cnomarchy.git

# 2. 进入认证服务目录
cd cnomarchy/services/auth

# 3. 运行一键部署脚本（把 api.yourname.com 换成你的域名）
sudo bash deploy.sh api.yourname.com
```

等待脚本运行完成（约 2-3 分钟）。

### 6.3 配置短信服务

部署完成后，编辑配置文件：

```bash
nano /opt/cnomarchy/auth/.env
```

你会看到类似这样的内容：

```
# 短信服务商配置
SMS_PROVIDER=aliyun
SMS_ACCESS_KEY_ID=your-access-key-id
SMS_ACCESS_KEY_SECRET=your-access-key-secret
SMS_SIGN_NAME=CNomarchy
SMS_TEMPLATE_CODE=SMS_123456789
```

用方向键移动光标，把这些值替换成你之前记录的：

- `SMS_ACCESS_KEY_ID`：你的 AccessKey ID
- `SMS_ACCESS_KEY_SECRET`：你的 AccessKey Secret
- `SMS_SIGN_NAME`：你创建的签名名称（比如 `CNomarchy`）
- `SMS_TEMPLATE_CODE`：你创建的模板编码（比如 `SMS_123456789`，在模板管理里可以看到）

修改完成后：
1. 按 `Ctrl + O` 保存
2. 按 `回车` 确认文件名
3. 按 `Ctrl + X` 退出

### 6.4 重启服务

```bash
systemctl restart cnomarchy-auth
```

### 6.5 验证服务

在浏览器打开：`http://api.yourname.com/docs`

如果能看到 API 文档页面，说明部署成功了！

---

## 7. 第五步：下载 CNomarchy 系统镜像

### 7.1 打开下载页面

在浏览器打开：https://github.com/zhaoshuny/cnomarchy/actions

### 7.2 找到最新的成功构建

1. 在列表中找到状态为 ✅ 绿色对勾的最新一次构建
2. 点击进入
3. 滚动到页面底部的 "Artifacts" 区域
4. 点击 `cnomarchy-iso` 下载

**注意**：文件比较大（约 4.7GB），下载需要一些时间。

### 7.3 如果下载慢

可以使用下载工具（比如 IDM、迅雷）加速下载。

---

## 8. 第六步：制作系统 U 盘

### 8.1 下载 Rufus

Rufus 是一个免费的 U 盘启动盘制作工具。

下载地址：https://rufus.ie/zh/

下载最新版本的 `rufus-x.x.exe`（单文件，不需要安装）。

### 8.2 制作启动盘

1. 插入 U 盘（至少 8GB，**U 盘上的数据会被全部删除，请先备份！**）
2. 双击运行 Rufus
3. 在"设备"中选择你的 U 盘
4. 在"引导类型选择"中点击"选择"，选中你下载的 CNomarchy ISO 文件
5. 其他选项保持默认
6. 点击"开始"
7. 弹出警告框，点击"确定"（确认 U 盘数据会被删除）
8. 等待制作完成（约 5-10 分钟）

---

## 9. 第七步：安装 CNomarchy 系统

### 9.1 从 U 盘启动

1. 把制作好的 U 盘插入要安装系统的电脑
2. 重启电脑
3. 在开机时快速按启动菜单键（不同品牌电脑按键不同）：
   - **联想**：F12 或 Fn+F12
   - **戴尔**：F12
   - **惠普**：F9
   - **华硕**：F8
   - **宏碁**：F12
   - **微星**：F11
   - **台式机**：一般按 Del 或 F2 进 BIOS，在启动项里选 U 盘
4. 在启动菜单中选择你的 U 盘（一般带有 USB 或 U 盘品牌名称）

### 9.2 试用系统

从 U 盘启动后，会进入 CNomarchy 系统（Live 模式，不会修改你的硬盘）。

你可以先试用一下，看看系统是否正常运行：
- 按 `Super + Return`（Windows 键 + 回车）打开终端
- 按 `Super + B` 打开浏览器
- 按 `Super + E` 打开文件管理器

### 9.3 安装到硬盘

如果决定安装，打开终端，输入：

```bash
sudo cnomarchy-install
```

这会启动 archinstall 安装向导，按照提示操作：

1. **选择语言**：选 `English`（安装过程用英文，安装完系统是中文的）
2. **选择地区/时区**：选 `Asia` → `Shanghai`
3. **选择键盘布局**：选 `us`
4. **选择磁盘**：选择你要安装系统的硬盘（**注意：会清空这个硬盘的数据！**）
5. **选择文件系统**：选 `ext4`
6. **设置主机名**：输入 `cnomarchy`（或你喜欢的名字）
7. **设置 root 密码**：设置一个密码（要记住！）
8. **创建用户**：
   - 用户名：`cnomarchy`（或你喜欢的名字）
   - 密码：设置一个密码
   - 确认密码：再输入一次
9. **选择桌面环境**：选 `Minimal`（我们已经预装了 Hyprland）
10. **选择额外软件包**：保持默认即可
11. **选择网络配置**：选 `Use NetworkManager`
12. **选择时区**：确认是 `Asia/Shanghai`
13. **选择自动时间同步**：选 `true`
14. **选择安装引导**：选 `grub`（如果是 BIOS）或 `systemd-boot`（如果是 UEFI）
15. **确认安装**：选 `Install`，按回车开始安装

等待安装完成（约 10-20 分钟），安装完成后重启电脑，拔掉 U 盘。

---

## 10. 第八步：配置手机号登录

### 10.1 首次登录

重启后，系统会自动登录（Live 模式默认自动登录，方便试用）。

### 10.2 配置认证服务器地址

打开终端，输入：

```bash
# 创建配置目录
mkdir -p ~/.cnomarchy

# 配置认证服务器地址（把 api.yourname.com 换成你的域名）
echo "API_BASE=https://api.yourname.com/v1" > ~/.cnomarchy/api_config
```

### 10.3 启用手机号登录

```bash
cnomarchy-enable-login
```

会显示：
```
✅ 手机号登录已启用
下次登录时将显示手机号登录界面
```

### 10.4 测试登录

1. 注销当前用户（或重启电脑）
2. 系统会显示手机号登录界面
3. 输入你的手机号，点击"发送验证码"
4. 手机会收到一条短信验证码
5. 输入验证码，点击"登录"
6. 登录成功后进入桌面

### 10.5 如果不想用手机号登录了

随时可以禁用：

```bash
cnomarchy-disable-login
```

---

## 11. 第九步：安装软件兼容层

CNomarchy 提供五层软件兼容，你可以根据需要安装。

### 11.1 查看兼容层状态

打开终端，输入：

```bash
cnomarchy-compat status
```

会显示各层兼容的安装状态。

### 11.2 安装所有兼容层

```bash
sudo cnomarchy-compat install all
```

### 11.3 只安装某一层

```bash
# L1: Flatpak 原生 Linux 应用（推荐安装）
sudo cnomarchy-compat install flatpak

# L2: Waydroid 安卓子系统（可以运行安卓 App）
sudo cnomarchy-compat install waydroid

# L3: Wine/Proton Windows 应用（可以运行 Windows 软件和游戏）
sudo cnomarchy-compat install wine

# L4: PWA 网页应用（把网站当桌面应用用）
sudo cnomarchy-compat install pwa

# L5: 云桌面远程工具（远程连接其他电脑）
sudo cnomarchy-compat install cloud
```

### 11.4 安装常用软件

```bash
# 用 Flatpak 安装软件（推荐）
flatpak search 软件名
flatpak install flathub 软件ID

# 用 pacman 安装软件
sudo pacman -S 软件名

# 搜索软件
pacman -Ss 关键词
```

---

## 12. 第十步：使用 AI Agent Harness

AI Agent Harness 是 CNomarchy 的核心创新，它让 AI 能够读取和控制你的操作系统。

### 12.1 启动 Harness 服务

```bash
# 进入 harness 目录
cd ~/cnomarchy/services/harness

# 安装依赖
pip install -r requirements.txt

# 启动服务（会自动生成一个 Token，记录下来）
python main.py
```

启动后会显示：
```
============================================================
  CNomarchy AI Agent Harness API 启动
============================================================
  API Token: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
  API 文档: http://localhost:8000/docs
  干运行模式: False
============================================================
```

**把这个 Token 记录下来，后面要用。**

### 12.2 查看 API 文档

打开浏览器访问：http://localhost:8000/docs

你会看到所有可用的 API 接口。

### 12.3 让 AI 帮你管理系统

你可以把 API 地址和 Token 告诉 AI 助手（比如 Claude、ChatGPT），让它帮你：

- 查看系统状态（CPU、内存、磁盘使用情况）
- 安装软件
- 管理服务
- 查看日志
- 修改配置文件

**示例提示词**：

```
我有一个 CNomarchy AI Agent Harness API：
- 地址：http://localhost:8000
- Token：你的Token

请帮我：
1. 查看当前系统的 CPU 和内存使用情况
2. 如果内存使用率超过 80%，帮我找出占用内存最多的 5 个进程
3. 给我一些优化建议
```

### 12.4 安全提示

- Harness API 默认只监听本地（127.0.0.1），外部无法访问
- 如果你想让远程 AI 访问，需要配置防火墙和 HTTPS
- 不要把 Token 告诉不信任的人
- 可以使用干运行模式测试：`CNOMARCHY_DRY_RUN=true python main.py`

---

## 13. 常见问题

### Q1: 安装系统时找不到 U 盘启动项

**A**：
1. 确认 U 盘制作成功（Rufus 显示"完成"）
2. 重启电脑，快速按启动菜单键（见 9.1 节）
3. 如果还是找不到，进 BIOS 设置，关闭"Secure Boot"（安全启动），保存后重启
4. 换一个 USB 口试试（优先用 USB 2.0 口，兼容性更好）

### Q2: 手机号登录收不到验证码

**A**：
1. 检查手机是否有信号，是否被拦截为垃圾短信
2. 检查短信服务的签名和模板是否审核通过
3. 检查 `.env` 配置文件中的 AccessKey、签名名称、模板编码是否正确
4. 检查服务器是否能访问外网（`ping www.baidu.com`）
5. 查看服务日志：`journalctl -u cnomarchy-auth -f`

### Q3: 系统没有声音

**A**：
1. 打开终端，输入 `alsamixer`，按 F6 选择声卡，用方向键调高音量，按 M 取消静音
2. 或者安装 `pavucontrol`：`sudo pacman -S pavucontrol`，然后运行 `pavucontrol` 调整

### Q4: 中文输入法不能用

**A**：
1. 按 `Ctrl + Space` 切换中英文
2. 如果还是不行，检查 Fcitx5 是否启动：`fcitx5 &`
3. 重启电脑试试

### Q5: 软件安装失败

**A**：
1. 先更新系统：`sudo pacman -Syu`
2. 更新软件源：`sudo pacman -Syy`
3. 如果是 Flatpak，更新 Flatpak：`flatpak update`
4. 检查网络连接是否正常

### Q6: 怎么连接 WiFi

**A**：
1. 点击顶部状态栏的网络图标
2. 选择你的 WiFi 名称
3. 输入密码
4. 或者在终端输入 `nmtui`，用文本界面连接

### Q7: 系统怎么更新

**A**：
```bash
sudo pacman -Syu
```

### Q8: 忘记密码怎么办

**A**：
1. 重启电脑，在 GRUB 启动菜单按 `e`
2. 找到 `linux` 那一行，在末尾加上 `init=/bin/bash`
3. 按 `Ctrl + X` 启动
4. 进入命令行后，输入 `passwd 用户名` 修改密码
5. 输入 `reboot -f` 重启

### Q9: 怎么卸载系统，恢复 Windows

**A**：
1. 用 Windows 安装 U 盘启动
2. 在安装界面选择"修复计算机"
3. 打开命令提示符，输入：
   ```
   bootrec /fixmbr
   bootrec /fixboot
   bootrec /rebuildbcd
   ```
4. 或者直接重新安装 Windows（会清空硬盘）

### Q10: AI Agent Harness 连不上

**A**：
1. 确认服务已启动：`python main.py`
2. 确认端口没被占用：`netstat -tlnp | grep 8000`
3. 确认防火墙没拦截：`sudo ufw allow 8000`
4. 浏览器访问 http://localhost:8000/docs 测试

---

## 🎉 完成！

恭喜你，你已经成功搭建了属于你的 CNomarchy 系统！

如果遇到问题，可以：
1. 查看本指南的"常见问题"部分
2. 在 GitHub 提交 Issue：https://github.com/zhaoshuny/cnomarchy/issues
3. 查看项目文档：https://github.com/zhaoshuny/cnomarchy

---

*最后更新：2026年8月*
