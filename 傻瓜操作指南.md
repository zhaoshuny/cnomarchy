# 🚀 CNomarchy 一键构建指南（完全不懂技术也能行）

## 你需要做的只有 5 步，每步不超过 1 分钟

---

## 第 1 步：注册 GitHub 账号（已有账号跳过）

1. 打开浏览器，访问：https://github.com
2. 点击右上角 **Sign up**（注册）
3. 用你的邮箱注册，设置密码
4. 验证邮箱
5. 登录进去

> 💡 GitHub 是免费的，我们用它的云服务器帮你构建操作系统，不花一分钱。

---

## 第 2 步：创建一个新仓库

1. 登录后，点击右上角的 **+** 号
2. 选择 **New repository**（新仓库）
3. 在 **Repository name**（仓库名）里输入：`cnomarchy`
4. 下面选 **Public**（公开）
5. 勾选 **Add a README file**（添加 README）
6. 点击最下面绿色的 **Create repository**（创建仓库）按钮

> ✅ 完成后你会看到一个空的仓库页面。

---

## 第 3 步：上传项目文件

1. 在仓库页面，点击 **Add file**（添加文件）按钮
2. 选择 **Upload files**（上传文件）
3. 打开文件管理器，找到 `cnomarchy` 文件夹
   - 位置：`此电脑 > C盘 > Users > Administrator > DoubaoWork > chats > 2026-08-26 > new-chat > cnomarchy`
4. **把整个 `cnomarchy` 文件夹拖进去**（拖到网页中间的虚线框里）
   - ⚠️ 是拖整个文件夹，不是里面的文件
   - 如果浏览器不支持拖文件夹，就打开文件夹，全选里面所有内容（Ctrl+A），拖进去
5. 等上传完成（上面会显示进度条）
6. 滚动到页面最下面，点击绿色的 **Commit changes**（提交更改）按钮

> ✅ 完成后仓库里就能看到所有文件了，包括 `.github`、`iso`、`scripts` 等文件夹。

---

## 第 4 步：触发自动构建

1. 在仓库页面顶部，点击 **Actions**（操作）标签
   - 如果弹出提示，点击 **I understand my workflows, go ahead and enable them**（我了解我的工作流，继续启用它们）
2. 左边会看到 **构建 CNomarchy ISO**
3. 点击右边的 **Run workflow**（运行工作流）按钮
4. 在弹出的小窗口里，分支选 `main` 或 `master`
5. 点击绿色的 **Run workflow** 按钮

> ✅ 构建开始了！页面上会出现一个黄色的转圈图标，表示正在构建。

---

## 第 5 步：等待并下载 ISO

1. **耐心等待 20-40 分钟**（构建需要时间，你可以去做别的事）
2. 回到 Actions 页面，当图标变成 **绿色对勾 ✅** 时，构建完成
3. 点击那个绿色对勾的构建记录（标题是 "构建 CNomarchy ISO"）
4. 滚动到页面**最底部**，找到 **Artifacts**（产物）区域
5. 点击 **cnomarchy-iso** 下载
6. 下载下来是一个 zip 包，解压后里面就是 `.iso` 文件和 `.sha256` 校验文件

> 🎉 **恭喜！你已经拥有了一个自己构建的操作系统 ISO 文件！**

---

## 怎么用这个 ISO

### 方法 A：用 Ventoy（推荐，最简单）

1. 准备一个 U 盘（至少 4GB，会被格式化）
2. 下载 Ventoy：https://www.ventoy.net/cn/download.html
3. 解压后运行 Ventoy2Disk.exe
4. 选择你的 U 盘，点击 **安装**
5. 安装完成后，把 ISO 文件直接复制到 U 盘里
6. 重启电脑，开机时按 F12（或 F2/Esc/Del，取决于电脑品牌）选择从 U 盘启动
7. 在 Ventoy 菜单里选择 CNomarchy 的 ISO，回车

### 方法 B：用虚拟机测试（不影响现有系统）

1. 下载 VirtualBox：https://www.virtualbox.org/
2. 安装后打开，点击 **新建**
3. 名字随便填，类型选 Linux，版本选 Arch Linux (64-bit)
4. 内存至少给 4096MB（4GB）
5. 虚拟硬盘至少给 32GB
6. 创建完成后，点击 **设置** → **存储** → 点击光盘图标 → **选择虚拟盘** → 选你的 ISO 文件
7. 点击 **启动**

---

## 默认登录信息

- **用户名**：`cnomarchy`
- **密码**：无（自动登录）
- **中文输入法**：按 `Ctrl + Space` 切换中英文
- **打开终端**：按 `Super` 键（Windows 键）+ `Enter`
- **打开浏览器**：按 `Super` + `B`
- **打开应用菜单**：按 `Super` + `Shift` + `Space`
- **查看帮助**：在终端输入 `cnomarchy-help`

---

## 常见问题

### Q: 构建失败了怎么办？
A: 点击失败的构建记录，查看日志，把红色的错误信息截图发给我，我来修代码，你重新上传再构建一次就行。

### Q: 构建要多久？
A: 通常 20-40 分钟，取决于 GitHub 服务器负载。第一次可能慢一些。

### Q: GitHub 免费吗？会不会扣费？
A: 完全免费。公开仓库的 Actions 每月有 2000 分钟免费额度，构建一次用 30 分钟，一个月能构建 60 多次，完全够用。

### Q: ISO 文件多大？
A: 大约 3-5GB。

### Q: 我想安装到硬盘上？
A: 从 ISO 启动进入系统后，在终端输入 `sudo cnomarchy-install`，按提示操作即可。

### Q: 这个系统能干嘛？
A: 这是一个基于 Arch Linux + Hyprland 的桌面操作系统，预装了中文输入法、浏览器、办公软件、开发工具、Wine（运行 Windows 软件）、Steam（玩游戏）等。适合日常使用和开发。

---

## 下一步

等你成功构建出 ISO 并启动后，告诉我：
1. 能不能正常进入桌面？
2. 中文输入法能不能用？
3. 有没有什么报错？

然后我们继续加功能：Omarchy 的 Quickshell 桌面、AI Agent、手机号登录、安卓子系统等。
